services:
  n8n:
    build:
      context: .
      dockerfile: Dockerfile
    command: sleep infinity
    volumes:
      - ..:/workspaces:cached
      - n8n_data:/workspaces/n8n_data
    env_file:
      - ../.env

volumes:
  n8n_data: