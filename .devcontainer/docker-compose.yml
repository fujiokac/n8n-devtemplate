services:
  n8n:
    build:
      context: .
      dockerfile: Dockerfile
    command: sleep infinity
    volumes:
      - ..:/workspace:cached
      - n8n_data:/workspace/n8n_data
    env_file:
      - ../.env

volumes:
  n8n_data: