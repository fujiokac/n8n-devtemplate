version: "3.9"

services:
  n8n:
    image: mcr.microsoft.com/devcontainers/javascript-node:18
    command: sleep infinity
    volumes:
      - ..:/workspace:cached
      - n8n_data:/workspace/n8n_data
    env_file:
      - ../.env
    depends_on:
      - db   # Placeholder – db service defined separately

volumes:
  n8n_data:
